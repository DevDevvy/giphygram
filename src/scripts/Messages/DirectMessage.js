//storing the values input into the message form 

//goes to database when send button clicked

//if current user is equal to recipient id then we display the count on the recipient users notification/nav bar

//and then generate the html of the message including sender/user and message body in message inbox

//also include boolean for read or unread message 
    //if read = false count the false and display message count in nav bar
    //if read = true then the page regenerates HTML to clear the message 